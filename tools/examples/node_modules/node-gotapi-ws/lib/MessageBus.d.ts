import MessageBody from './MessageBody';
import SocketBus from './SocketBus';
import Direction from './Direction';
import SortedHash from './SortedHash';
import MessageBodyCallback from './MessageBodyCallback';
import ServerAckCallback from './ServerAckCallback';
declare class MessageBus {
    direction: Direction;
    socketBus: SocketBus;
    private sendStatusHash;
    sortedHash: SortedHash | null;
    private historyUrl;
    private groupName;
    historySize: number;
    position: number;
    constructor(direction: Direction, socketBus: SocketBus, historyUrl: string, groupName: string, historySize: number, position: number);
    pushMessage(msg: string): void;
    onServerAck(uuid: string): void;
    pullHistory(): void;
    onMessage(messageStr: string): void;
    all(): MessageBody[];
    setNewMessageCallback(callback: MessageBodyCallback): void;
    setServerAckCallback(callback: ServerAckCallback): void;
    setRemoveCallback(callback: ServerAckCallback): void;
    close(): void;
}
export default MessageBus;
