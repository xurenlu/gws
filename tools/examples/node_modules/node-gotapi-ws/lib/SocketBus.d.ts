import Message from './Message';
interface OnMessageImpl {
    (data: any): void;
}
interface OnCloseImpl {
    (event: any): void;
}
declare class SocketBus {
    _id: string;
    ws: WebSocket;
    websocketAddr: string;
    onClose: OnCloseImpl;
    onMsg: OnMessageImpl;
    wsPath: string;
    private closed;
    private timer;
    onConnect: any;
    constructor(websocketAddr: string);
    pingpong(): void;
    send(data: Message): void;
    connect(): void;
    close(): void;
}
export default SocketBus;
